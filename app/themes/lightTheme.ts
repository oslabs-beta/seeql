const lightTheme = {
  button: { color: 'coral' }
};

export default lightTheme;

//create the class that holds the wrappers: create two base themes: light/dark
//write an onclick handler for the toggler:trigger theme state change
// components need to become them
